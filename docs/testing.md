<!-- GENERATED by awf: do not edit; change .awf/ and run `awf render` -->
# Testing

This document is the repository-specific authority for test commands, feedback tiers, lane ownership, and expected runtime. Keep concrete setup and measurements here rather than relying on a shared convention that cannot reflect the repository's language, dependencies, or execution environment.

Every behaviour-changing fix requires the strongest practical durable oracle. The normal and preferred path is an automated regression test observed failing for the right reason and then passing. When that path is impractical, state a concrete reason, preserve or improve verification strength, and retain the strongest safe, reproducible alternative. Never weaken expected behaviour. Never weaken verification strength. Fix the root cause rather than the symptom.

Use this evidence order as guidance, not a requirement to mechanically attempt every earlier option:

1. An automated regression test observed red then green.
2. A deterministic integration or reproduction harness.
3. A contract or invariant test that directly exercises the failure.
4. Use scripted, reproducible manual verification with recorded inputs and expected result.
5. An explicit explanation of why durable automation is unavailable, plus the strongest safe evidence that can be retained.

For a nondeterministic race, stress or invariant evidence may be the strongest practical oracle. For a destructive migration defect, use safe fixture or dry-run evidence rather than unsafe reproduction. An alternative is legal because the preferred path is impractical, not merely inconvenient, and its reason and retained evidence must make any verification-strength judgment reviewable.

<!-- awf:edit gate: from .awf/docs/parts/testing/gate.md -->
## Gate
Run `./scripts/check` before every commit. It runs Python pytest, verifies that the checked-in Pi formula schema matches the authoritative Python request model, runs Pyright and Ruff, runs Pi TypeScript lint/type checks, formatting and Vitest, then runs `./awf check`. Vitest excludes managed awf worktrees so an integration gate runs each Pi test once. Run the slower `./scripts/check-release` after render settlement to verify a temporary clean snapshot, separate Git pins, production installation, and ready and disabled Pi resource behavior.


<!-- awf:edit tiers: from .awf/docs/parts/testing/tiers.md -->
## Tiers and lanes
Python tests cover the transport-free public formula API, qualified equation systems, AFMM-like acceptance, and distribution. `packages/pi-science/tests/` exercises the actual full-request adapter, strict Pi schema and rich result shapes, whole-envelope and output bounds, AFMM system translation through the registered callback, installed-extension schema compilation by the host Pi runtime, stderr draining, process failure, timeout and cancellation cleanup, readiness withholding, immutable checkout identity, package contents, and prerequisite-specific diagnostics. A barrier wrapper around real uv proves concurrent healthy starts reuse one immutable-source cache without modifying the checkout; a synchronized failure proves identical fail-closed results. The combined gate confines transient test data to an external user-cache directory and removes it on exit; direct Pi test runs also remove their temporary package, install, provisioning, and checkout fixtures.

The Pi tier includes protocol-v17 analysis and candidate-comparison request/result parity, a query-bearing AFMM tail round trip, and partial nested finite-polynomial closed-form transport separate from preserved submitted-work fixtures. Comparison coverage proves strict result variants, correlation, syntax injection, and semantic-before-work presentation without duplicating Python mathematics.

Derived query targets cross protocol v17 as strict correlation data only; Python owns earlier-only validation, verified-candidate eligibility, bounded qualification composition, and unavailable-target results.

The Python and Pi tiers cover canonical dominance decomposition, poles, exact cells, bounded abstention, and strict dominance transport.


Bounded aggregate-work dominance is available in Python and Pi protocol v17. Python owns its canonical rational decomposition and exact one-axis regions; Pi validates and presents the report without recomputing mathematical policy. It excludes multiple axes, exponentials, opaque aggregates, rewrites, resource vectors, scheduling, and empirical performance.


Optimization coverage fixes the strict explicit goal/search/proof literals, `1..16` projection limit, absence from ordinary analysis, preserve-all submitted-domain policy, both objectives, every shipped exact lane, positional cross-equation interfaces, bounded Horner generation, scope and capture refusals, localized blockers without extra work, every independent search budget, deterministic repeated-process ordering, independent proof evidence, whole-work values, result classification, search completion, ranked-prefix selection, projection truncation, schema generation, real-adapter and strict-bridge round trips, and the 262,144-byte optimization-output allowance. Exact-algorithmic regressions cover ADR-0012 boundary refusals, shared checked derivation with unchanged query answers, independent parent and original-final identity checks, positive whole-computation savings including output multiplicity, and mixed two-tier replay. Protocol-v17 tests mutate goal, claim, scope, classification, blocker, selection, and projection fields independently. TypeScript validates and correlates transported policy without deriving sums, refusals, algebra, objectives, or ranking.


<!-- awf:edit layout: from .awf/docs/parts/testing/layout.md -->
## Layout and test shape
Python tests cover the transport-free public formula API and distribution. Contract identity and package probes cover canonical `contracts` definitions and compatibility forwards; neutral-analysis tests cover retained construction and structural occurrences; `tests/unit/optimization/` covers optimizer families, replay, verification, objectives, canonical state, search, and bounds; query and scenario suites cover service-owned orchestration. Dependency probes enforce the Python owner graph and Pi's outward-only bridge barrel. `packages/pi-science/tests/` exercises the actual private adapter, strict envelopes and result shapes, UTF-8 request and response bounds, installed-extension schema compilation and tool invocation under the host Pi runtime, stderr draining, process failure, timeout and cancellation cleanup, real Pi command/tool result contracts, mode-aware warnings, immutable checkout identity, health validation, and prerequisite-specific diagnostics. A barrier wrapper around real uv proves two healthy concurrent starts reuse one immutable-source cache without modifying the checkout; a separate synchronized failure proves identical fail-closed results.

Pi regressions cover protocol-v17 analysis, explicit goal optimization, and candidate-comparison requests, exact discriminated result shapes, malformed transport, correlation, named constrained-domain reports, and registered-tool round trips. Candidate tests preserve mapped semantics, original aggregate work, second-minus-first delta, syntax injection, and semantic-before-work compact output without TypeScript mathematical recomputation.

Derived query targets cross protocol v17 as strict correlation data only; Python owns earlier-only validation, verified-candidate eligibility, bounded qualification composition, and unavailable-target results. Focused regressions cover the partial direct nested finite-polynomial closed-form family and preserve submitted work separately from its informational candidate.

`tests/e2e/test_formula_dominance.py` covers the direct-Python dominance contract and preserves ordinary nested analysis.


Bounded aggregate-work dominance is available in Python and Pi protocol v17. Python owns its canonical rational decomposition and exact one-axis regions; Pi validates and presents the report without recomputing mathematical policy. It excludes multiple axes, exponentials, opaque aggregates, rewrites, resource vectors, scheduling, and empirical performance.


`tests/unit/optimization/` and the expression/system e2e suites cover required goal/search/proof literals, absence from ordinary analysis, submitted-domain and preserve-all semantics, typed occurrence scope, every shipped exact lane, checked intermediate substitution, proof evidence, work correlation, localized blockers, deterministic selection, independent budget exhaustion, observed result classification, and conservative omission. Exact-algorithmic tests use the `test_exact_algorithmic_sum_v1_` prefix and cover query parity, the narrow nested finite-polynomial boundary, structural path retention, independent transition/final proof, selected-objective arithmetic, mixed traces, and mutation rejection. Schema, adapter, bridge, provisioning, start, package, and documented-example tests cover protocol v17, goal and claim literals, scope and blocker correlation, populated and empty classifications, search and projection independence, no-runtime wording, the optimization-output allowance, and unchanged ordinary results.

### Capability evidence index

The [analysis model](analysis-model.md) owns behavior. This index points to representative regression evidence and changes with the fixture population.

| Capability | Representative evidence |
|---|---|
| Public Python exports | [`tests/unit/test_public_exports.py`](../tests/unit/test_public_exports.py), `test_public_formula_root_exports_are_complete_unique_and_importable` |
| Expression analysis | [`tests/e2e/test_formula_analysis.py`](../tests/e2e/test_formula_analysis.py), `test_analyze_returns_normalized_interpretation` |
| Named equation systems | [`tests/e2e/test_formula_system_analysis.py`](../tests/e2e/test_formula_system_analysis.py), `test_afmm_like_request_reports_structural_work_scenarios_and_uncertainty` |
| Scenarios | [`tests/unit/test_formula_scenarios.py`](../tests/unit/test_formula_scenarios.py), `test_scenarios_preserve_general_and_report_fixed_choices_asymptotic_and_bounds` |
| Supported query families | [`tests/unit/test_formula_queries.py`](../tests/unit/test_formula_queries.py), `test_equivalence_conclusions_and_empty_default`, `test_closed_form_series_rule_matrix_and_afmm_identity`, and `test_later_consumers_preserve_exact_answer_shape_and_check_order` |
| Every derived query consumer | [`tests/unit/test_formula_queries.py`](../tests/unit/test_formula_queries.py), `test_nested_polynomial_direct_consumers_and_explicit_derived_reuse` |
| Candidate comparison | [`tests/e2e/test_formula_candidate_comparison.py`](../tests/e2e/test_formula_candidate_comparison.py), `test_expression_candidates_disprove_before_work_preference` and `test_exact_sign_chart_reports_work_crossover` |
| Aggregate-work dominance | [`tests/e2e/test_formula_dominance.py`](../tests/e2e/test_formula_dominance.py), `test_integer_correction_contract_and_pair_signs` and `test_unknown_unsupported_and_nonfinite_work_abstain` |
| Repeated subexpression | [`tests/unit/optimization/test_families.py`](../tests/unit/optimization/test_families.py), `test_local_optimization_families_publish_only_verified_savings` |
| Repeated call | [`tests/unit/optimization/test_families.py`](../tests/unit/optimization/test_families.py), `test_repeated_defined_call_is_reused_but_unknown_call_is_omitted`; [`packages/pi-science/tests/adapter.test.ts`](../packages/pi-science/tests/adapter.test.ts), `uses all fixed optimization families, including algorithmic finite sums` |
| Reciprocal reuse | [`tests/unit/optimization/test_families.py`](../tests/unit/optimization/test_families.py), `test_scope_collision_reciprocal_conditions_and_incompatible_sums`; [`packages/pi-science/tests/adapter.test.ts`](../packages/pi-science/tests/adapter.test.ts), `uses all fixed optimization families, including algorithmic finite sums` |
| Factoring | [`tests/e2e/test_formula_analysis.py`](../tests/e2e/test_formula_analysis.py), `test_complete_candidate_replays_factoring_neutral_and_horner_with_context` |
| Redundant-operation removal | [`tests/unit/optimization/test_families.py`](../tests/unit/optimization/test_families.py), `test_neutral_redundant_operations_can_reduce_work_to_zero` |
| Iterator-invariant hoisting | [`tests/unit/optimization/test_families.py`](../tests/unit/optimization/test_families.py), `test_hoisting_with_no_whole_work_improvement_is_omitted`; [`packages/pi-science/tests/adapter.test.ts`](../packages/pi-science/tests/adapter.test.ts), `uses all fixed optimization families, including algorithmic finite sums` |
| Cross-equation sharing | [`tests/unit/optimization/test_families.py`](../tests/unit/optimization/test_families.py), `test_cross_equation_sharing_and_horner_publish_verified_savings` |
| Horner reformulation | [`tests/e2e/test_formula_analysis.py`](../tests/e2e/test_formula_analysis.py), `test_dense_polynomial_horner_advice_is_independently_proved_and_lower_work` |
| Exact-algorithmic finite sum | [`tests/unit/optimization/test_algorithmic_sum.py`](../tests/unit/optimization/test_algorithmic_sum.py), `test_exact_algorithmic_sum_v1_mixes_with_algebraic_search_deterministically`; [`packages/pi-science/tests/adapter.test.ts`](../packages/pi-science/tests/adapter.test.ts), `uses all fixed optimization families, including algorithmic finite sums` |
| Explicit goal and ordinary-analysis separation | [`tests/unit/optimization/test_goals.py`](../tests/unit/optimization/test_goals.py), `test_explicit_goal_request_requires_fixed_goal_search_proof_and_projection` and `test_ordinary_analysis_has_no_optimization_field`; [`tests/unit/optimization/test_replay.py`](../tests/unit/optimization/test_replay.py), `test_ordinary_analysis_has_no_optimization_field_or_optimizer_dispatch` |
| Complete replay | [`tests/unit/optimization/test_replay.py`](../tests/unit/optimization/test_replay.py), `test_complete_candidate_replays_expression_local_reuse_and_neutral_removal` and `test_composed_search_v1_emits_replayable_trace` |
| Objectives | [`tests/unit/optimization/test_objectives.py`](../tests/unit/optimization/test_objectives.py), `test_objective_v1_default_all_one_parity_and_stable_identity` and `test_objective_v1_weighted_power_reverses_plan_order` |
| Search incompleteness and fair population | [`tests/unit/optimization/test_budgets.py`](../tests/unit/optimization/test_budgets.py), `test_each_independent_search_budget_preserves_base_success`; [`tests/unit/optimization/test_search.py`](../tests/unit/optimization/test_search.py), `test_composed_search_v1_retained_lanes_are_round_robin_and_order_invariant` |
| Output projection and combined qualifications | [`tests/unit/optimization/test_budgets.py`](../tests/unit/optimization/test_budgets.py), `test_optimize_result_bound_keeps_largest_fitting_prefix` and `test_result_byte_bound_preserves_projection_limit_qualification` |
| Stable plan identity and canonical states | [`tests/unit/optimization/test_objectives.py`](../tests/unit/optimization/test_objectives.py), `test_objective_v1_default_all_one_parity_and_stable_identity`; [`tests/unit/optimization/test_canonical_states.py`](../tests/unit/optimization/test_canonical_states.py), `test_composed_search_v1_alpha_renamed_binders_keep_population_order` and `test_composed_search_v1_equation_permutations_keep_logical_population` |
| Generated schema | [`tests/test_pi_schema_generation.py`](../tests/test_pi_schema_generation.py), `test_generated_pi_schema_is_deterministic_and_current` and `test_generated_pi_schema_has_public_expression_and_system_branches` |
| Real adapter transport | [`packages/pi-science/tests/adapter.test.ts`](../packages/pi-science/tests/adapter.test.ts), `returns explicit v17 goal-driven plans with claims, scope, and blockers` and `keeps incomplete search scope distinct from projection status` |
| Strict bridge correlation and malformed envelopes | [`packages/pi-science/tests/bridge.test.ts`](../packages/pi-science/tests/bridge.test.ts), `strictly correlates protocol-v17 goal optimization results` and `strictly rejects malformed envelopes and result shapes` |
| Pi presentation | [`packages/pi-science/tests/start.test.ts`](../packages/pi-science/tests/start.test.ts), `presents explicit goal optimization claims, scope, projection, and blockers` and `keeps ordinary analysis free of optimization presentation` |
| Client stdout and stderr consumption | [`packages/pi-science/tests/bridge.test.ts`](../packages/pi-science/tests/bridge.test.ts), `consumes and bounds large stderr on a failed child` and `bounds retained output and cleans a resistant high-volume producer` |
| Provisioning and readiness | [`packages/pi-science/tests/provision.test.ts`](../packages/pi-science/tests/provision.test.ts), `uses real uv concurrently against one immutable source and external cache`; [`packages/pi-science/tests/start.test.ts`](../packages/pi-science/tests/start.test.ts), `discovers the product skill only with the ready analysis tool` |
| Python packaging and imports | [`tests/distribution/test_python_package.py`](../tests/distribution/test_python_package.py), `test_built_distribution_is_standalone_namespace_package` |
| Pi packaging and installed startup | [`packages/pi-science/tests/package.test.ts`](../packages/pi-science/tests/package.test.ts), `packs the required Pi sources under AGPL and production-installs externally` |

