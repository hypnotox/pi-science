prefix: pi-science
integrationBranch: main
vars:
    gateCmd: ./scripts/check
    testCmd: uv run --locked pytest
bootstrap:
    enabled: true
localDocs:
    - name: vision
      title: Vision
      description: Project purpose, scope, principles, and intended users
    - name: analysis-model
      title: Analysis Model
      description: Mathematical requests, scenarios, analysis results, and uncertainty qualifications
domains:
    - product
